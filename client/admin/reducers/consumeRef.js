const initConsumeRefState = {
  // TODO: refinery API 확인 후 작업 예정
};

////////////////////////////////////////////////////////////////////////////////////
// REDUCER - CONSUME REFINERY
//
export default function consumeRef(state = initConsumeRefState, action) {
  /*switch (action.type) {
    case 'INIT_REF':
      return {
        ...state
      };
    case 'RUN_REF':
      return {
        ...state
      };
    case 'STOP_REF':
      return {
        ...state
      };
    case 'REMOVE_REF':
      return {
        ...state
      };
    default:
      return state;
  }*/
  return state;
}